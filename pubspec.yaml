name: HackChat
description: HackChat client
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  webview_flutter:
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  url_launcher: ^5.7.10
  cupertino_icons: ^1.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.1
  build_runner: ^2.1.5
  flutter_lints: ^1.0.0
flutter:
  uses-material-design: true

